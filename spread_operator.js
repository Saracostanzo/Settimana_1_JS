// scrivi una funzione che accetti un array di numeri e applichi alcune operazioni sugli elementi utilizzando lo Spread Operator.

// Funzione con Spread Operator:

// Implementa una funzione chiamata gestisciArrayNumeri che accetti un array di numeri come argomento.
// Operazioni sugli Array:

// All'interno della funzione, utilizza almeno due metodi degli array per manipolare gli elementi dell'array. Ad esempio, potresti utilizzare Array.includes() per verificare se un certo numero è presente e Array.map() per raddoppiare ciascun elemento.
// Stampa dei Risultati:

// Stampa l'array originale di numeri.
// Stampa un nuovo array ottenuto duplicando ciascun numero.
// Stampa se un numero specifico (a tua scelta) è presente nell'array.



function gestisciArrayNumeri(numeri) {
    // Stampa l'array originale

  //.....
  
    // Crea un nuovo array duplicando ciascun numero(utilizza lo spread operator) e stampalo in console

   //....
  
    // Specifica un numero da cercare nell'array

    //....
  
    // Verifica se il numero specifico è presente nell'array e stampalo in console

   //....
  }
  
  // Esempio di utilizzo
  gestisciArrayNumeri([3, 5, 7, 9]);
  // Output atteso:
  // Array originale: [3, 5, 7, 9]
  // Array duplicato: [6, 10, 14, 18]
  // Il numero 7 è presente: true
  